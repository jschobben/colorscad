use <common.scad>

// As of v0.4.1, no geometry is produced.
// This should be fixed in a future version (expected color is red).
echo("This produces no output yet, so a warning from ColorSCAD is expected");
color("red") color("green") tetra();
